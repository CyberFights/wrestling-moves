# Copy this file to `.env` for local development.
# On Railway this is provided automatically — attach a PostgreSQL plugin and
# reference its DATABASE_URL, or set it manually under Variables.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db
